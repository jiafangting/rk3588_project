# 7. 语音模块（嵌入式岗位重点版）

这一阶段不要把重点放在“语音算法有多复杂”上。

你投的是嵌入式岗位，所以语音模块重点学：

```text
语音模块在系统里是什么角色
voice_loop.py 启动后做了什么
声音怎么变成文字
文字怎么匹配成本地指令
语音模块怎么通过 Socket 问视觉模块
回复文字怎么播报出来
大模型什么时候介入
为什么大模型不能直接控制设备
```

一句话：

```text
语音模块是人机交互入口，不是核心报警决策模块。
```

核心报警决策仍然在：

```text
RK3588 C 主控
```

语音模块主要负责：

```text
听用户说话
把语音转成文字
判断用户想干什么
查询状态
触发巡检保存
把结果播报出来
本地指令没命中时，用大模型兜底
```

---

## 7.1 语音模块整体流程

入口文件是：

```text
voice/voice_loop.py
```

核心类是：

```text
VoiceAssistant（语音助手主类）
```

可以把它理解成：

```text
语音模块的总调度器
```

整体流程是：

```text
语音模块启动
  ↓
初始化语音识别器
  ↓
初始化视觉模块客户端
  ↓
初始化语音播报器
  ↓
初始化大模型对象
  ↓
进入循环监听
  ↓
录音
  ↓
语音识别成文字
  ↓
处理文字指令
  ↓
生成回复
  ↓
语音播报
```

你要记住：

```text
语音模块自己不生产系统状态。
它主要是问别的模块，然后把结果说出来。
```

比如：

```text
问视觉状态
  ↓
通过 Socket 问视觉模块

问有没有报警
  ↓
查询实时状态和报警记录

让它截图
  ↓
通知视觉模块保存当前画面
```

所以语音模块的定位是：

```text
交互入口
```

不是：

```text
主控大脑
```

---

## 7.2 录音、静音检测和唤醒词

这一节对应：

```text
怎么监听麦克风
怎么判断用户说完了
怎么进入工作模式
```

语音模块启动后默认是：

```text
待机模式
```

待机模式下，不是什么话都响应。

它主要等待：

```text
唤醒词
```

比如：

```text
小杜你好
```

识别到唤醒词后，才进入：

```text
工作模式
```

完整流程是：

```text
默认待机
  ↓
监听麦克风
  ↓
检测到用户说话
  ↓
录音
  ↓
检测到一段时间安静
  ↓
认为用户说完了
  ↓
保存音频
  ↓
语音识别
  ↓
如果是唤醒词，进入工作模式
  ↓
工作模式下才处理具体命令
```

这里有一个重要函数：

```text
record_until_silence（录音直到静音函数）
```

它的作用是：

```text
检测到声音就开始录
检测到安静一段时间就停止录
```

不是固定录很久。

可以这样理解：

```text
声音大于阈值：认为用户正在说话
声音低于阈值并持续一段时间：认为用户说完了
```

代码里还有一个：

```text
WORK_MODE_TIMEOUT_SECONDS（工作模式超时时间）
```

当前是：

```text
20 秒
```

意思是：

```text
进入工作模式后，如果 20 秒没有有效输入，就自动回到待机模式。
```

这样可以减少误触发。

---

## 7.3 离线语音识别

这一节解决的问题是：

```text
录音文件怎么变成文字？
```

语音识别就是：

```text
ASR（语音识别）
```

你可以这样记：

```text
ASR（语音识别）：人说话 -> 文字
```

你的项目里用的是：

```text
faster-whisper（离线语音识别库）
```

对应代码在：

```text
voice/voice_loop.py
```

核心类是：

```text
WhisperRecognizer（语音识别器）
```

它的作用是：

```text
输入：录音文件
输出：识别出来的文字
```

例如：

```text
输入：一段用户语音
输出：当前温度多少
```

它只负责把声音转文字。

它不负责：

```text
判断是不是温度查询
判断是不是报警查询
控制设备
触发巡检
```

这些是后面的本地指令解析负责。

代码流程可以理解成：

```text
第一次使用时加载模型
  ↓
读取录音文件
  ↓
指定中文识别
  ↓
过滤静音片段
  ↓
拼接识别结果
  ↓
返回文字
```

模型名称当前是：

```text
small
```

你只需要知道：

```text
模型越大，通常越准，但更慢；
模型越小，速度更快，但更容易识别错。
```

对嵌入式岗位来说，重点不是讲识别算法，而是讲：

```text
语音识别本地运行
核心设备指令不依赖大模型
语音模块挂了不影响 C 主控报警闭环
```

---

## 7.4 本地指令解析

这一节是语音模块最重要的代码逻辑。

它解决的问题是：

```text
语音已经变成文字了，程序怎么知道用户想干什么？
```

核心函数是：

```text
handle_text（文字处理函数）
build_reply（回复生成函数）
```

可以这样理解：

```text
handle_text（文字处理函数）：判断当前模式，决定要不要处理这句话
build_reply（回复生成函数）：真正判断这句话是什么命令
```

流程是：

```text
语音识别输出文字
  ↓
handle_text（文字处理函数）
  ↓
判断是否停止词
  ↓
判断是否唤醒词
  ↓
进入工作模式
  ↓
build_reply（回复生成函数）
  ↓
本地关键词匹配
  ↓
调用对应功能
  ↓
生成回复文本
```

在匹配之前，会先做：

```text
normalize_text（文本归一化函数）
```

它的作用是：

```text
修正常见识别错误
去掉空格和标点
方便关键词匹配
```

比如：

```text
寻检、巡警、寻衅
```

可能都会尽量修正成：

```text
巡检
```

本地规则主要匹配这些命令：

```text
温度查询
湿度查询
电流查询
电压查询
烟雾状态查询
设备状态查询
报警查询
视觉状态查询
巡检保存
拍照截图
进入待机
退出程序
```

比如：

```text
文字里有“温度”
  ↓
查询温度

文字里有“报警”
  ↓
查询报警

文字里有“巡检 / 拍照 / 截图”
  ↓
通知视觉模块保存记录

文字里有“待机 / 休息”
  ↓
切回待机模式
```

这里最重要的一句话：

```text
设备相关命令优先走本地规则。
```

为什么？

因为这些命令是确定性的：

```text
查温度
查报警
截图
巡检
待机
```

不能让大模型自由发挥。

所以你的项目设计是：

```text
先本地规则
再大模型兜底
```

---

## 7.5 语音模块和视觉模块通信

语音模块不直接处理摄像头。

它通过 Socket 问视觉模块。

对应文件是：

```text
voice/vision_control.py
vision/app/vision_socket.py
```

角色是：

```text
视觉模块：服务端
语音模块：客户端
```

服务端就是：

```text
一直等别人来问的人
```

客户端就是：

```text
主动去问的人
```

语音模块主要发两类命令。

### 1. 查询视觉状态

当用户说：

```text
当前视觉状态怎么样
有没有视觉报警
```

语音模块会发：

```json
{"cmd":"get_status"}
```

意思是：

```text
请把当前最新视觉状态发给我。
```

视觉模块返回的数据大概包括：

```text
status（视觉状态）
person_count（人数）
zone_hit（是否进入禁区）
alarm_type（报警类型）
alarm_reason（报警原因）
timestamp（时间）
```

然后语音模块把这些字段整理成中文播报。

### 2. 触发巡检保存

当用户说：

```text
开始巡检
帮我截图
拍照
保存当前画面
```

语音模块会发：

```json
{"cmd":"trigger_inspection"}
```

意思是：

```text
请你保存当前画面，并生成一条巡检记录。
```

注意：

```text
不是语音模块自己截图。
```

真正保存图片和记录的是：

```text
视觉模块
```

所以这里的分工是：

```text
语音模块：发命令
视觉模块：执行截图和记录保存
```

和 C 主控对比一下：

```text
C 主控问视觉模块：
为了融合决策，判断要不要报警。

语音模块问视觉模块：
为了回答用户，或者触发一次巡检保存。

UI 问视觉模块：
为了界面显示。
```

所以可以记：

```text
视觉模块是公共服务。
不同模块都可以问它，但目的不同。
```

---

## 7.6 语音播报和大模型兜底

语音播报就是：

```text
TTS（语音合成）
```

你可以这样记：

```text
TTS（语音合成）：文字 -> 机器说话
```

对应文件是：

```text
voice/voice_broadcast.py
```

核心类是：

```text
VoiceBroadcaster（语音播报器）
```

它负责：

```text
把回复文字放进播报队列
后台线程慢慢播报
```

为什么要用队列？

因为播报一句话需要时间。

如果主流程一直等播报结束，语音模块就会卡住。

所以当前设计是：

```text
主流程生成回复
  ↓
把回复放入播报队列
  ↓
主流程继续监听
  ↓
后台播报线程负责说出来
```

这叫：

```text
异步播报
```

说人话就是：

```text
说话这件事交给后台做，主程序不用一直等。
```

当前项目里优先用：

```text
pyttsx3（语音合成库）
```

如果失败，会尝试：

```text
PowerShell 系统播报兜底
```

---

## 7.7 大模型是怎么接入的

大模型对应文件是：

```text
voice/llm_chat.py
```

核心类是：

```text
LLMChat（大模型聊天类）
```

在语音主流程中创建：

```text
self.llm = LLMChat()
```

意思是：

```text
语音模块里接了一个大模型对象。
```

大模型是否可用，看：

```text
self.llm.enabled（大模型是否启用）
```

它主要依赖这些环境变量：

```text
DASHSCOPE_API_KEY（大模型密钥）
LLM_MODEL（模型名称）
LLM_BASE_URL（大模型接口地址）
```

可以这样理解：

```text
DASHSCOPE_API_KEY：有没有权限调用大模型
LLM_MODEL：使用哪个模型
LLM_BASE_URL：请求发到哪个大模型服务
```

如果没有配置密钥：

```text
self.llm.enabled = False
```

这时：

```text
本地语音指令仍然能用
只是在线问答不能用
```

---

## 7.8 本地规则和大模型的关系

这是第 7 阶段最重要的面试点。

一句话：

```text
本地规则负责可靠执行，大模型负责理解模糊表达。
```

完整顺序是：

```text
用户说话
  ↓
语音识别成文字
  ↓
先走本地关键词规则
  ↓
如果命中，直接执行本地函数
  ↓
如果没命中，再看大模型是否可用
  ↓
大模型先做意图识别
  ↓
如果识别成设备指令，回到本地函数执行
  ↓
如果只是普通聊天，才让大模型自由回答
```

对应函数链路：

```text
build_reply（回复生成函数）
  ↓
本地关键词匹配
  ↓
reply_by_llm_intent（大模型意图识别回复函数）
  ↓
classify_intent（意图分类函数）
  ↓
识别成设备意图就回到本地函数
  ↓
识别成普通聊天才调用 ask（普通问答函数）
```

比如用户说：

```text
帮我看看现在安不安全
```

这句话不一定有“报警”两个字。

本地规则可能没匹配上。

这时大模型可以判断：

```text
用户其实想问报警状态
```

然后程序回到：

```text
reply_alarm_query（报警查询函数）
```

去查真实状态和报警记录。

也就是说：

```text
大模型只帮忙理解这句话像什么命令。
真正查数据和执行动作的，还是本地代码。
```

---

## 7.9 为什么大模型不能直接控制设备

因为大模型可能：

```text
理解错
回答不稳定
编造实时数据
把模糊话理解成危险动作
```

比如用户问：

```text
现在温度多少？
```

如果让大模型直接回答，它可能编出：

```text
当前温度 26 摄氏度。
```

但这个数字不一定是真实传感器数据。

正确做法是：

```text
大模型判断用户想查温度
  ↓
本地函数读取真实温度
  ↓
再播报真实结果
```

对于这些设备相关动作，更不能让大模型直接控制：

```text
报警
继电器
蜂鸣器
截图
巡检
待机
退出程序
```

所以你的设计应该讲成：

```text
大模型只做意图理解，不直接控制硬件。
```

可以记成：

```text
大模型只当翻译，不当司机。
```

意思是：

```text
它可以把用户的话翻译成系统能懂的意图；
真正控制设备、读取状态、写日志的，必须是本地程序。
```

---

## 7.10 这一阶段应该看哪些代码

不用全看。

按这个顺序看：

```text
1. voice/voice_loop.py
看 VoiceAssistant（语音助手主类）
看 run（运行主循环）
看 handle_text（文字处理函数）
看 build_reply（回复生成函数）
看 reply_by_llm_intent（大模型意图识别回复函数）

2. voice/vision_control.py
看 get_status（查询视觉状态）
看 trigger_inspection（触发巡检保存）

3. voice/voice_broadcast.py
看 VoiceBroadcaster（语音播报器）
看 speak（播报函数）
看播报队列

4. voice/llm_chat.py
看 LLMChat（大模型聊天类）
看 enabled（大模型是否启用）
看 classify_intent（意图分类函数）
看 ask（普通问答函数）
```

可以少看或者不细看：

```text
具体录音底层细节
faster-whisper 内部算法
pyttsx3 内部实现
大模型 API 底层协议
```

因为这些不是嵌入式岗位重点。

---

## 7.11 面试表达

可以这样讲：

```text
语音模块作为独立 Python 进程运行，主要负责人机交互，不参与核心报警决策。模块启动后通过麦克风录音和 faster-whisper 离线语音识别将用户语音转换为文字，然后优先通过本地关键词规则解析温湿度查询、报警查询、视觉状态查询、巡检触发、待机切换等确定性指令。需要视觉状态或巡检保存时，语音模块作为 Socket 客户端访问视觉模块，发送 get_status 或 trigger_inspection 命令。回复文本通过 TTS 队列异步播报，避免阻塞主循环。
```

关于大模型可以这样讲：

```text
语音模块接入大模型作为兜底能力，但大模型不直接控制设备。系统优先使用本地规则处理确定性设备指令；当本地规则未命中时，大模型只用于意图识别，把模糊表达映射到本地函数，例如报警查询或视觉状态查询。实时状态读取和设备动作仍由本地代码完成，避免大模型编造数据或产生不确定控制。
```

如果面试官问语音模块挂了会不会影响报警，可以说：

```text
不会影响核心报警闭环。语音模块只是交互入口，核心传感器采集、视觉查询、融合决策和报警执行仍由 RK3588 C 主控和 STM32 侧完成。
```

---

## 7.12 一句话总结

```text
第 7 阶段重点不是学语音算法，而是学语音模块如何把用户语音变成文字，先走本地规则执行可靠设备指令，再用大模型兜底理解模糊表达，并通过 TTS 把结果播报出来。
```
