# 8. PyQt5 上位机（嵌入式岗位重点版）

这一阶段不用把重点放在 PyQt5 控件怎么写上。

你投的是嵌入式岗位，所以 UI 只需要学到：

```text
UI 在系统里是什么角色
UI 从哪里拿数据
UI 怎么刷新状态
UI 怎么显示报警和画面
UI 怎么修改阈值配置
UI 和 C 主控、视觉模块的边界在哪里
```

一句话：

```text
UI 是上位机展示层，不是核心控制大脑。
```

真正的核心控制在：

```text
RK3588 C 主控
```

真正的视觉识别在：

```text
Python 视觉模块
```

UI 主要负责：

```text
显示系统状态
显示摄像头画面
显示传感器数值
显示报警记录
修改阈值配置
```

---

## 8.1 UI 的定位

UI 可以理解成：

```text
工业巡检系统的上位机看板
```

它不直接做这些事情：

```text
不直接采集 STM32 传感器
不直接控制蜂鸣器、LED、继电器
不直接做融合决策
不直接运行 YOLO 识别
不直接决定最终是否报警
```

它主要做这些事情：

```text
把后端状态显示出来
把最新画面显示出来
把报警历史显示出来
给用户提供阈值修改入口
```

所以你看 UI 代码时，不需要纠结每个按钮和布局怎么写。

你要重点看：

```text
它从哪里读数据
它多久刷新一次
它修改配置后通知了谁
```

---

## 8.2 UI 的整体结构

入口文件是：

```text
ui/main_window.py
```

里面的核心类是：

```text
MainWindow（主窗口类）
```

可以把它理解成：

```text
整个上位机界面的外壳
```

主窗口里面主要有这些页面：

```text
总览页面
视觉监控页面
报警历史页面
系统设置页面
```

每个页面由不同的小面板组成：

```text
CameraWidget（摄像头画面面板）
SensorWidget（传感器状态面板）
AlarmWidget（当前报警面板）
AlarmHistoryWidget（报警历史面板）
ThresholdWidget（阈值配置面板）
VoiceWidget（语音交互显示面板）
```

你不用背这些控件细节。

你只要知道：

```text
MainWindow（主窗口类）负责把这些面板拼起来。
```

---

## 8.3 UIState 是什么

重点文件是：

```text
ui/data/ui_state.py
```

核心对象是：

```text
UIState（界面状态对象）
```

它是 UI 这一层最重要的东西。

你可以把它理解成：

```text
UI 的统一数据中转站
```

为什么需要它？

因为如果每个面板都自己去读 Socket、读日志、读配置文件，代码会很乱。

所以当前项目的设计是：

```text
UIState（界面状态对象）统一取数据
  ↓
各个界面面板只负责显示
```

也就是：

```text
UIState 负责拿数据
Widget 负责显示数据
```

这点是 UI 代码里最应该掌握的。

---

## 8.4 UI 从哪里拿数据

UI 的数据来源主要有 4 类。

### 1. 视觉 Socket

UI 会通过 Socket 问视觉模块：

```json
{"cmd":"get_status"}
```

意思是：

```text
请把当前最新视觉状态发给我。
```

返回的数据可能包括：

```text
status（视觉状态）
person_count（人数）
zone_hit（是否进入禁区）
alarm_type（报警类型）
alarm_reason（报警原因）
live_frame_path（最新画面路径）
```

注意：

```text
get_status（查询状态命令）只是读取视觉模块缓存的最新结果。
它不会重新触发一次 YOLO 识别。
```

### 2. 图片文件

UI 显示摄像头画面时，主要读取：

```text
vision/app/output/photos/live_frame.jpg
```

这个文件是视觉模块保存的最新画面。

所以画面链路是：

```text
视觉模块打开摄像头
  ↓
视觉模块处理画面
  ↓
保存 live_frame.jpg（最新画面文件）
  ↓
UI 读取这个图片
  ↓
显示在界面上
```

重点：

```text
UI 正常情况下不直接占用摄像头。
```

这样可以避免 UI 和视觉模块抢摄像头资源。

### 3. 日志文件

UI 会读取报警和巡检记录，比如：

```text
rk3588/alarm_log.csv
vision/app/output/recodes/records.csv
```

这些文件用于显示：

```text
报警历史
最近报警原因
报警截图路径
历史巡检记录
```

你可以这样区分：

```text
Socket 负责“现在怎么样”
日志文件负责“以前发生过什么”
```

### 4. 配置文件

UI 会读取和修改：

```text
rk3588/config.json
```

里面放的是报警阈值，比如：

```text
温度上限
湿度上下限
电压上下限
电流上限
设备温度上限
烟雾报警阈值
```

UI 修改阈值，本质上就是：

```text
把新的阈值写回 config.json
```

---

## 8.5 UI 怎么刷新

主窗口里有一个定时器：

```text
refreshTimer（刷新定时器）
```

它大约每 500ms 调用一次：

```text
refreshUI（刷新界面函数）
```

刷新流程可以理解成：

```text
定时器触发
  ↓
UIState.refresh（刷新界面状态）
  ↓
查询视觉 Socket
  ↓
读取报警日志
  ↓
读取 config.json
  ↓
各个面板更新显示
```

所以 UI 不是后端主动推送所有数据过来。

当前项目里更像是：

```text
UI 定时去取最新状态
```

---

## 8.6 摄像头画面显示要记什么

摄像头显示对应文件：

```text
ui/widgets/camera_widget.py
```

核心面板：

```text
CameraWidget（摄像头画面面板）
```

它主要做：

```text
读取 live_frame.jpg
调整图片大小
叠加状态信息
显示到界面上
```

它可能会显示：

```text
当前状态
人数
禁区框
人员框
报警边框
时间戳
```

但是这些只是展示。

真正的图像识别仍然在：

```text
视觉模块
```

所以你可以这样记：

```text
视觉模块负责看和识别
UI 负责显示识别结果
```

---

## 8.7 阈值配置要记什么

阈值配置对应文件：

```text
ui/widgets/threshold_widget.py
```

它主要做：

```text
读取 config.json
显示当前阈值
用户修改阈值
保存回 config.json
发送 reload_config（重新加载配置命令）
```

这里要注意当前项目的现实情况：

```text
UI 修改 config.json 后，配置文件确实变了。
```

但 C 主控是否立刻生效，取决于 C 主控有没有重新读取配置。

如果 C 主控只在启动时读取一次：

```text
修改 config.json 后，需要重启 C 主控，或者给 C 主控增加 reload_config 接口。
```

所以面试时不要说得太满。

可以说：

```text
UI 侧提供阈值修改入口，修改后写入 config.json。
当前工程中后端需要重新加载配置后，新的阈值才会参与后续报警判断。
```

如果你后续补充 C 主控 reload_config 接口，就可以说：

```text
UI 保存阈值后通知 C 主控重新加载配置，实现阈值实时生效。
```

---

## 8.8 报警记录显示要记什么

报警显示主要分两类：

```text
当前报警状态
历史报警记录
```

当前报警状态主要来自：

```text
视觉 Socket 返回的状态
日志中的最近状态
```

历史报警记录主要来自：

```text
rk3588/alarm_log.csv
vision/app/output/recodes/records.csv
```

UI 只负责读出来显示。

它不负责判断“这次到底要不要报警”。

判断报警的是：

```text
RK3588 C 主控的融合决策线程
```

---

## 8.9 当前 UI 和核心系统的边界

你一定要分清楚：

```text
UI 不是核心控制模块
```

系统边界可以这样理解：

```text
STM32：
负责传感器采集和执行器控制

RK3588 C 主控：
负责融合决策和报警执行调度

视觉模块：
负责摄像头采集、视觉识别、截图记录、视觉 Socket 服务

UI：
负责显示状态、显示记录、修改配置
```

所以 UI 挂了，理论上不应该影响核心报警闭环。

也就是说：

```text
没有 UI，系统也应该能继续巡检和报警。
```

这句话很适合嵌入式面试。

---

## 8.10 这一阶段你应该看哪些代码

不用全看。

建议只看这些：

```text
ui/main_window.py
看主窗口如何创建 UIState 和各个面板

ui/data/ui_state.py
重点看 UIState.refresh（界面状态刷新函数）

ui/widgets/camera_widget.py
重点看它怎么读 live_frame.jpg

ui/widgets/threshold_widget.py
重点看它怎么读写 config.json
```

可以少看或者不细看：

```text
具体布局代码
颜色样式代码
字体大小代码
按钮美化代码
表格样式代码
```

因为这些不是嵌入式岗位重点。

---

## 8.11 面试表达

可以这样讲：

```text
上位机界面基于 PyQt5 实现，主要作为状态展示和配置入口，不参与核心报警决策。UI 侧设计了 UIState 作为统一数据层，周期性通过 Socket 查询视觉模块状态，并读取最新画面文件、报警日志和 config.json 配置文件，把实时画面、传感器数据、报警状态和历史记录展示出来。阈值配置由 UI 写入 config.json，后端重新加载后参与后续报警判断。核心决策仍由 RK3588 C 主控完成，UI 与底层控制解耦。
```

如果面试官追问 UI 和后端怎么通信，可以说：

```text
UI 主要通过视觉模块提供的 Socket 查询实时视觉状态，通过文件读取最新画面、报警日志和阈值配置。Socket 用来获取当前状态，文件用于保存历史记录和配置。
```

如果面试官追问 UI 是否直接控制硬件，可以说：

```text
UI 不直接控制 STM32 执行器，也不直接做融合决策。执行器控制由 STM32 完成，融合决策由 RK3588 C 主控完成，UI 只提供展示和配置入口。
```

---

## 8.12 一句话总结

```text
第 8 阶段重点不是学 PyQt5 控件，而是学 UI 如何作为上位机展示层，从 Socket、图片、日志和配置文件中获取数据，并和核心控制逻辑保持解耦。
```
