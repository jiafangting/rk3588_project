# 软件仿真流程学习日志

这份日志是写给“小白”的，目标不是背代码，而是先看懂：
**仿真后端怎么产生数据、UI 怎么读数据、语音怎么问视觉、C 主控怎么做报警判断。**

---

## 1. 整体链路先记住

你可以先把整个项目理解成一条流水线：

```text
仿真后端 / 真实硬件
    -> 产生传感器和视觉状态
    -> 决策线程判断是否报警
    -> 写日志 / 提供 socket
    -> UI 读取后展示
    -> 语音模块读取后播报
```

现在没有硬件，所以 `scripts/run_software_simulation.py` 就在扮演前半段：
- 模拟 STM32 传感器；
- 模拟视觉识别；
- 模拟报警判断；
- 模拟 socket；
- 模拟日志文件。

---

## 2. `scripts/run_software_simulation.py` 是怎么工作的

这是仿真后端，核心可以分成 5 步。

### 第一步：定义场景

文件里有 `SCENARIOS`。每个场景其实就是一组“预设值”。
例如：
- `normal`：正常场景；
- `abnormal_empty_area`：画面里没检测到人；
- `smoke_alarm`：烟雾报警；
- `zone_alarm`：进入禁区；
- `current_alarm`：电流过载。

你可以把它理解成“脚本化演示模式”。
仿真器并不是每次都随机乱跑，而是在这些场景之间轮换，这样更容易测试 UI 和语音。

### 第二步：给传感器加一点随机波动

`add_noise()` 会在基础值上加一点小浮动。

这样做的原因很简单：
- 如果数值永远不变，界面看起来像死掉了；
- 加一点波动，更像真实设备读数。

比如温度会从 28.0 变成 28.2、27.9 之类。

### 第三步：做报警判断

`decide_alarm()` 是最重要的函数之一。
它按照顺序检查：

1. 温度是否超过 `temp_max`
2. 湿度是否低于 `humidity_min`
3. 湿度是否高于 `humidity_max`
4. 电流是否超过 `current_max`
5. 设备温度是否超过 `temp_device_max`
6. 烟雾是否触发
7. 视觉状态是否是 `ALARM`
8. 视觉状态是否是 `ABNORMAL`
9. 否则就是 `NORMAL`

也就是说，这里是“传感器优先，视觉补充”的逻辑。

### 第四步：把结果写出去

每次更新会写三种东西：

- socket 状态：给 UI / 语音 / C 主控读；
- `records.csv` / `records.jsonl`：给视觉历史记录用；
- `rk3588/alarm_log.csv`：给主控日志和 UI 用。

这就是为什么 UI 断开也没关系，因为数据已经落到文件里了。

### 第五步：生成一张预览图

`write_live_frame()` 会画一张简单图片保存到 `live_frame.jpg`。
UI 左边的画面就是从这里读的。

如果机器上没有 OpenCV，这一步可能失败，但不影响 socket 和日志功能。

---

## 3. `ui/data/ui_state.py` 是怎么工作的

这个文件可以理解为 UI 的“数据中枢”。

### 为什么要有 `UIState`

因为 UI 页面上有很多地方要显示数据：
- 视觉状态；
- 传感器值；
- 报警记录；
- 阈值配置；
- 画面路径。

如果每个组件都自己去读文件，就会很乱。
所以这里统一由 `UIState.refresh()` 拉取一次。

### `refresh()` 的顺序

它会按这个顺序刷新：

1. 去视觉 socket 问当前状态；
2. 读报警日志；
3. 读阈值文件；
4. 如果都失败，就生成模拟数据。

也就是说，UI 优先读真实/仿真数据，实在不行才退回假数据。

### UI 为什么能一直显示

因为它不只依赖 socket。
它还会读：
- `rk3588/alarm_log.csv`
- `vision/app/output/recodes/records.csv`
- `rk3588/config.json`

所以只要其中一个来源还在，UI 就能继续工作。

---

## 4. `voice/vision_control.py` 是怎么工作的

这个文件是语音模块和视觉模块的“中间人”。

它本身不识别图像，只做两件事：

- `get_status()`：问当前视觉状态；
- `trigger_inspection()`：让视觉模块立刻保存一条记录。

### 为什么它重要

因为语音模块说“有没有报警”的时候，不能自己瞎猜。
它要去问视觉模块，再把返回结果整理成中文句子。

比如：
- 状态是 `NORMAL` -> 说“正常”；
- 状态是 `ABNORMAL` -> 说“异常”；
- 状态是 `ALARM` -> 说“报警”。

### 它怎么通信

它发的是 JSON 命令，比如：

```json
{"cmd": "get_status"}
```

这和仿真后端、C 主控是同一套协议。
这很重要，因为统一协议以后，后面换成真机也不用重写语音那部分。

---

## 5. `rk3588/thread_decision.c` 是怎么工作的

这是 C 版本的决策线程。
如果你想理解“真机逻辑”，这里就是重点。

### 这一线程在干什么

它每 500ms 读一次系统状态，然后判断：
- 传感器是否超阈值；
- 视觉是否报警；
- 报警是否解除。

### 判断顺序

它的逻辑和 Python 仿真版其实很像：

1. 温度过高 -> 报警
2. 湿度异常 -> 报警
3. 电流过载 -> 报警
4. 设备温度过高 -> 报警
5. 烟雾报警 -> 报警
6. 视觉报警 -> 报警
7. 如果上次报警，这次恢复正常 -> 解除报警

### 为什么要看这个文件

因为它告诉你：
仿真不是随便乱写的，Python 版本只是把 C 版逻辑“翻译”了一遍，方便你没硬件时先跑通。

---

## 6. 建议你怎么学

如果你是第一次看这个项目，我建议顺序是：

1. 先看 `scripts/run_software_simulation.py` 的 `SCENARIOS`
2. 再看 `decide_alarm()`
3. 再看 `update_once()`
4. 再看 `ui/data/ui_state.py` 的 `refresh()`
5. 再看 `voice/vision_control.py`
6. 最后看 `rk3588/thread_decision.c`

这样不会一上来就被 C、Python、socket、日志文件同时吓到。

---

## 7. 你可以怎么运行来验证理解

先开仿真后端：

```powershell
python scripts/run_software_simulation.py --reset
```

再开 UI：

```powershell
python ui/main_window.py
```

再开语音文本模式：

```powershell
python voice/voice_loop.py --text
```

然后你可以观察：
- 场景切换时 UI 状态是否变化；
- 报警时日志是否增加；
- 语音里输入 `vision` 是否能读到视觉状态；
- 阈值改掉后，报警是否跟着变。

---

## 8. 一句话总结

这套系统的核心不是“某一个文件很神奇”，而是：

- 仿真后端负责造数据；
- UI 负责读数据和展示；
- 语音负责问数据和播报；
- C 主控负责真正做报警逻辑。

你现在最重要的目标不是一次看懂全部，而是先搞清楚：
**数据从哪里来，怎么流到 UI，怎么流到语音，最后怎么变成报警。**
