# Models

Put model weights in this directory.

Expected default file:

```text
models/yolo11n/yolo11n.pt
```

The source code does not need the full Ultralytics repository here. Keep only
the model weights used by the project.
